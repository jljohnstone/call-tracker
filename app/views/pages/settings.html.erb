<div>
  <h1 class="font-bold text-2xl">Settings</h1>
  <% if signed_in? %>
    <%= current_user.email %>
    <%= button_to "Sign out", sign_out_path, method: :delete, class: "btn btn-accent btn-sm" %>
  <% else %>
    <%= link_to "Sign in", sign_in_path %>
  <% end %>
</div>
