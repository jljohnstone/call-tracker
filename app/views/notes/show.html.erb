<div>
  <div class="flex justify-between items-center border-b-2 pb-3 gap-6">
    <h1 class="font-bold text-2xl flex-initial">Call Note <%= @note.id %></h1>
  </div>

  <%= render @note %>

  <div class="flex gap-3">
    <%= link_to 'Edit this note', edit_note_path(@note), class: "flex-auto btn btn-primary" %>
    <%= link_to "Cancel", :back, class: "flex-initial btn" %>
  </div>
</div>
