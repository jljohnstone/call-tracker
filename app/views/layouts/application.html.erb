<!DOCTYPE html>
<html>
  <head>
    <title>CallTracker</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <nav class="flex gap-6 p-6 bg-blue-100">
      <%= link_to "Calls", phone_calls_path, class: "bg-gray-300 rounded uppercase py-2 px-3 font-bold text-xs hover:bg-gray-400" %>
      <%= link_to "Customers", customers_path, class: "bg-gray-300 rounded uppercase py-2 px-3 font-bold text-xs hover:bg-gray-400" %>
    </nav>
    <main class="container mx-auto p-6 flex">
      <%= yield %>
    </main>
  </body>
</html>
